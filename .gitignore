# Build artifacts
/bin/
coverage.out
coverage.html

# Local AI-assistant tooling configs (personal, not shipped)
.aider.conf.yml
.aider*

# Local-dev TLS certs (generated by deploy/gen-dev-certs.sh)
deploy/certs/*.pem
deploy/certs/*.key

# Stray cmd binaries from a bare `go build ./cmd/<x>` (build to ./bin instead)
/auth-oidc
/imapd
/mail-remote
/mail-session
/pop3d
/queue-manager
/session-manager
/smtpd
/userctl
/webadmin
